import { Building2, UserRound } from 'lucide-react';
import type { ExpedienteDetail } from '../../expedientes.types';
import styles from '../../Expedientes.module.css';
export function PartiesTab({ expediente }: { expediente: ExpedienteDetail }) { return <section className={styles.sectionCard}><header><div><h2>Comparecientes</h2><p>Personas vinculadas a esta operación y su carácter.</p></div></header>{expediente.comparecientes?.length ? <div className={styles.partyList}>{expediente.comparecientes.map((item: any) => { const physical = item.compareciente?.personaFisica; const legal = item.compareciente?.personaMoral; const name = physical?.nombre_completo_calculado || legal?.razon_social || item.compareciente?.nombre_busqueda; const Icon = legal ? Building2 : UserRound; return <article key={item.id}><span><Icon size={19} /></span><div><strong>{name}</strong><small>{item.caracter?.nombre || 'Carácter no registrado'} · {String(item.forma_comparecencia || '').replaceAll('_', ' ').toLocaleLowerCase('es-MX')}</small></div><b className={item.datos_validados ? styles.completeText : styles.pendingText}>{item.datos_validados ? 'Validado' : 'Pendiente'}</b></article>; })}</div> : <p className={styles.sectionEmpty}>No hay comparecientes vinculados.</p>}</section>; }
