# RBAC / IDOR externo

Estado: **NOT EXECUTED — EXTERNAL STAGING REQUIRED**.

Matriz requerida: Dirección, Administración, Abogado A/B, Recepción, Gestoría y Consulta, tanto API directa como UI. Conteos externos: 0/0. La evidencia 15C local no se reutiliza como evidencia externa.

Al desbloquear se ejecutará `e2e:rbac` con mutaciones habilitadas únicamente contra el ref staging verificado y datos sintéticos con actor explícito.
